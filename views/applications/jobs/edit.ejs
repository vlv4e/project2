<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Job Post</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('../../partials/navbar.ejs') %>
  <h1>Edit <%= currentJob.jobTitle %> at <%= currentJob.company %></h1>
  <form action="/jobs/<%= currentJob._id %>?_method=PUT" method="POST">

        <label for="jobTitle">Job Title:</label>
        <input type="text" id="jobTitle" value="<%= currentJob.jobTitle %>" name="jobTitle" required>
        <label for="jobType">Job Type:</label>
        <select id="jobType"  name="jobType" required>
            <option value="Full-time" <%= currentJob.jobType === "Full-time" ? "selected" : "" %>
                >Full-time</option>
            <option value="Part-time" <%= currentJob.jobType === "Part-time" ? "selected" : "" %>>Part-time</option>
            <option value="Contract" <%= currentJob.jobType === "Contract" ? "selected" : "" %>>Contract</option>
            <option value="Internship" <%= currentJob.jobType === "Internship" ? "selected" : "" %>>Internship</option>
        </select>
        <label for="department">Department:</label>
        <input type="text" id="department" value="<%= currentJob.department %>" name="department" required>
        <label for="period">Period:</label>
        <select id="period" name="period" required>
            <option value="Immediate" <%= currentJob.period === "Immediate" ? "selected" : "" %>>Immediate</option>
            <option value="1 month" <%= currentJob.period === "1 month" ? "selected" : "" %>>1 month</option>
            <option value="2 months" <%= currentJob.period === "2 month" ? "selected" : "" %>>2 months</option>
            <option value="3 months" <%= currentJob.period === "3 month" ? "selected" : "" %>>3 months</option>
        </select>
        <label for="expectedSalary">Expected Salary:</label>
        <input type="number" id="expectedSalary" value="<%= currentJob.expectedSalary %>" name="expectedSalary" required min="0">
        <label for="experience">Experience:</label>
        <select id="experience" name="experience" required>
            <option value="Fresher" <%= currentJob.experience === "Fresher" ? "selected" : "" %>>Fresher</option>
            <option value="1-2 years" <%= currentJob.experience === "1-2 years" ? "selected" : "" %>>1-2 years</option>
            <option value="3-5 years" <%= currentJob.experience === "3-5 years" ? "selected" : "" %>>3-5 years</option>
            <option value="5+ years" <%= currentJob.experience === "5+ years" ? "selected" : "" %>>5+ years</option>
        </select>
        <label for="location">Location:</label>
        <input type="text" id="location" value="<%= currentJob.location %>" name="location" required>
        <label for="jobDescription">Job Description:</label>
        <textarea id="jobDescription" name="jobDescription" required minlength="10"><%= currentJob.jobDescription %></textarea>
        <button type="submit">Save</button>
    </form>
</body>
</html>








